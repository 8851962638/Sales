// Example script to handle button clicks and display product sales
function showProductSales(product, sales) {
    document.getElementById('total-sales').innerText = `Total Sales for ${product}: $${sales}`;
}
